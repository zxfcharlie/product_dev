# 二期修改说明

1. SKU 自动编号
   - 新建 SKU 自动生成：`gzs-yymmdd00001`
   - 同一天按现有最大序号 +1，例如 `gzs-26090200001`、`gzs-26090200002`
   - SKU 编号不再人工填写/修改

2. 表格单元格直接编辑
   - 点击可编辑单元格直接进入输入/下拉编辑
   - 单选字段使用下拉框，多选字段使用多选下拉，数字/日期/文本直接填写
   - Enter/失焦保存，Esc 取消（部分下拉选择后直接保存）

3. SKU 类目同步配置表
   - SKU 商品类目选项实时读取“品类负责人配置表”的一级类目 + 二级类目并去重
   - 普通成员可以使用类目选项，但不可查看配置表

4. AI/套图/上架任务负责人轮流分配修复
   - 负责人支持英文逗号、中文逗号、分号、换行分隔
   - 使用数据库行锁 + 强制 JSONB 脏标记 + flush，保证 `next_assign_index` 持久化
   - 多人同时创建任务时也按顺序推进

5. 用户管理与权限
   - 管理员侧边栏增加“用户管理”
   - 支持用户备注直接编辑和删除用户
   - 删除用户时保留历史业务记录，只解除创建人/视图归属
   - 禁止删除当前登录账号；至少保留一个管理员
   - 所有配置表仅管理员可见，后端 API 同时做 403 权限校验
   - 启动时自动给旧数据库 users 表补充 `note` 字段，无需手工迁移
