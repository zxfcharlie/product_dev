# 复制为 .env 并修改
POSTGRES_PASSWORD=change_me_to_a_strong_password
SECRET_KEY=change_me_to_a_long_random_string

# 部署在 Apache/Nginx + HTTPS 后面时改成 true，登录 cookie 只在 HTTPS 下发送，更安全；
# 如果暂时还没配好 HTTPS、只用 http://IP:8000 测试访问，保持 false（否则登录会失败）。
COOKIE_SECURE=false
